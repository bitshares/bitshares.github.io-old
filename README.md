Installation
==============

    cd bitshares.github.io
    gem install jekyll
    gem install bundler
    bundler install
    gem install github-pages

Building
=================

    bundle exec jekyll build

Running this page
=================

    bundle exec jekyll serve
