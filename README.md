Installation
==============

    cd bitshares.github.io
    gem install bundler
    bundle install

Building
=================

    bundle exec jekyll build

Running this page
=================

    bundle exec jekyll serve
